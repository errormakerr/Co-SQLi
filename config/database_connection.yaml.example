# The repository's database_connection.yaml contains the deployment values.
# Keep the password in the environment rather than in versioned YAML.
host: '127.0.0.1'
port: 13306
user: 'YOUR_READ_ONLY_USER'
password_env: COSQLI_MYSQL_PASSWORD
charset: 'utf8mb4'
