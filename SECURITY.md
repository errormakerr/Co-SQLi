# Security Policy

Co-SQLi is intended for isolated, authorized SQL-injection research. Run the
MySQL sidecar only against the supplied disposable runtime and do not point it
at production databases, credentials, or network services.

Keep API keys, database passwords, model paths, runtime configuration, and
generated artifacts outside the repository. Report security issues privately to
the repository maintainers rather than including exploit details in a public
issue.
